import 'package:flutter/material.dart';
import 'Theme/Implementation.dart';
import 'Pages/goodPage.dart';

void main() {
  runApp(Application());
  // runApp(ConsumerPage());
  // runApp(NotifierPage());
}
